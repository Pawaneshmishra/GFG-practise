int countSumSubsets(int arr[], int N){
        
        for(int i=0; i<N; i++)
        {
            if(arr[i]%2!=0)
            {
                return (pow(2,N-1))-1;
            }
        }
        
        return (pow(2,N))-1;
    }
